# Session Handoff — 2026-08-17

## File Modified

`Enterprise-Agentic-AI-Tutorial/01-Agentic-AI-Fundamentals.md`
Total lines after session: **2017**

---

## What Was Done This Session

All work was on **Section 3** (Core Concepts) of the fundamentals module. Two sections were fully fleshed out from diagram-only stubs, and a chain of foundational concept explanations was added as the session progressed from user questions.

---

## Section-by-Section Changes

### 3.4 Agent Lifecycle (lines 211–326)

**Before:** Mermaid state diagram only, no explanatory text.

**Added:**
- Intro paragraph framing the lifecycle as a stateful iterative process
- **Phase-by-Phase Breakdown table** — maps each state (Idle → Perceiving → Planning → Acting → Observing → Responding) to inputs, outputs, and latency budgets
- **State Transitions in Detail** — happy path iteration heuristic (2–5 is normal; >10 is a signal); two exception paths (HITL trigger conditions; error classification + retry table)
- **Lifecycle Instrumentation** — minimum OpenTelemetry event schema for production tracing with an alerting tip for infinite-loop detection
- **Lifecycle vs. Concurrency** — single agent is sequential; sub-agents vs. parallel tool calls as parallelism patterns
- **Interview question block** — canonical answer on how an agent knows when to stop

---

### 3.5 Memory Architecture (lines 327–1011)

**Before:** One-line intro + Mermaid diagram only.

This section grew significantly. All additions listed in order:

| Subsection Added | What It Covers |
|---|---|
| **Four Memory Types table** | Analogy, lifespan, read/write pattern, storage backing for each type |
| **Real-World Examples** | One concrete scenario per memory type (IT support, financial analysis, HR policy, SaaS sales agent) |
| **RAG and Semantic Memory — Same Thing?** | RAG = retrieval mechanism (verb); semantic memory = storage concept (noun); two divergence cases |
| **What Is an Embedding Model?** | Text → fixed-size vector; cosine similarity; pipeline diagram; model comparison table; core trade-off |
| **Dimensions — detailed breakdown** | float32 × dims = KB math; corpus-scale storage table; why search slows; diminishing returns quality curve; curse of dimensionality; why the model is a long-lived dependency (primary key analogy) |
| **What Is a Neural Network?** | Structure diagram; training (backpropagation + gradient descent); types table (Transformer, Encoder-only, Encoder-Decoder, CNN); link to embedding models; parameters deep-dive |
| **Parameters in LLMs** | What a parameter is; internal LLM structure (Q/K/V/O, feed-forward, output projection); parameter count vs. capability table; parameters vs. dimensions comparison table |
| **What Is a CNN?** | Full form; kernel/filter sliding mechanism; layer table (Conv, Pool, FC); ASCII filter diagram; early vs. deep layer features; agent use-case table |
| **How Each Memory Type Works** | Context window layout (ASCII token budget); episodic read strategies + Redis write code; semantic retrieval code (ChromaDB + sentence-transformers) |
| **Context Window Management** | Sliding window vs. summarization vs. hierarchical retrieval trade-off table |
| **Read/Write Access Matrix** | Which memory type the agent reads/writes vs. external pipelines |
| **Writing to Semantic Memory** | When to write (5 triggers); ingestion pipeline diagram; code example with delete-before-upsert pattern; chunking strategies table; metadata fields explained |
| **Interview Questions (Q1–Q8)** | 8 questions covering: episodic vs. semantic, context window limitations, missing episodic store implementation, risks of agent writing to semantic memory, context overflow strategies, in-context vs. episodic cost/latency/durability, RAG vs. semantic memory, when/how semantic memory is written |

---

## Key Decisions Made

| Decision | Reason |
|---|---|
| **Removed all Azure-specific references from examples** | User requested general (cloud-agnostic) examples |
| **Episodic code example uses Redis** | Generic, widely understood, not cloud-specific |
| **Semantic code example uses ChromaDB + sentence-transformers** | Open-source, cloud-agnostic, illustrates the concept clearly |
| **`Contoso Cloud` → `Acme SaaS`** | Remove Microsoft sample company reference |
| **`azure-pipelines.yml` → `ci-pipeline.yml`** | Remove Azure DevOps-specific filename |
| **`Azure AI Foundry plugins` → `packaged skill modules`** | Generic framing |
| **Token cap stated as range (128k–200k) not GPT-4o specific** | Model-agnostic |

---

## Concepts Explained In-Session (User Questions → Added to Doc)

1. **Does RAG represent semantic memory?** — RAG is the verb, semantic memory is the noun; added as subsection + Q7
2. **What is an embedding model?** — Added as `#### What Is an Embedding Model?`
3. **What is a neural network?** — Added as `#### What Is a Neural Network?`
4. **Full form of CNN** — Added as `#### What Is a CNN (Convolutional Neural Network)?`
5. **Diff between embedding models and LLM** — Explained in session; *not yet added to the document*
6. **What are dimensions in an embedding model?** — Added within embedding model section
7. **What are parameters in LLMs?** — Added within neural network section
8. **Detailed explanation of dimension trade-off table** — Expanded in-place with math, scale tables, search cost analysis, diminishing returns, and the primary-key analogy

---

## Pending / Not Yet Added to Document

- **Embedding model vs. LLM comparison** — Was explained in session (generator vs. compressor; decoder-only vs. encoder-only; output is tokens vs. vector; training objective; size and cost) but the user did not confirm adding it. Consider adding as a subsection after `#### What Is an Embedding Model?` or as an interview question.

---

## Suggested Next Steps

- [ ] Add embedding model vs. LLM comparison section (pending confirmation)
- [ ] Continue with **3.6 Tool / Function Calling** — currently has diagram only, same pattern as 3.4 and 3.5 before this session
- [ ] Sections 4.x (LLM Reasoning, Token Budget, Failure Modes, Autonomy Spectrum) — may need the same treatment
- [ ] Section 5.x uses Azure-specific CLI and code — decide whether to generalise or keep Azure-specific (this is an Azure tutorial)
- [ ] Review interview questions at end of document (lines ~1937–2017) for consistency with new Q&A style used in 3.4 and 3.5
